import { Outlet } from "react-router-dom";
import Layout from "./Layout";

const Home = () => {
  return (
    <div>
      {/* <nav>Navigation Bar</nav> */}
      <div>{/* Login or Signup buttons */}</div>
      <Layout />
    </div>
  );
};

export default Home;
